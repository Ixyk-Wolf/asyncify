from setuptools import setup

setup(
    name="asyncify",
    version=1.0,
    description="Make a blocking function asynchronous",
    python_requires=">=3.6",
    url="https://github.com/Ixyk-Wolf/asyncify"
)